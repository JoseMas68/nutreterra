---
// Hero section para la página principal
---

<section class="relative min-h-[85vh] flex items-center overflow-hidden">
  <!-- Imagen de fondo con efecto Parallax suave -->
  <div class="absolute inset-0 z-0">
    <img
      src="https://images.unsplash.com/photo-1490645935967-10de6ba17061?w=1920&q=80"
      alt="Healthy Food Art"
      class="w-full h-full object-cover scale-105 animate-slow-zoom"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-black/80 via-black/40 to-transparent"></div>
  </div>

  <!-- Contenido -->
  <div class="container mx-auto px-4 relative z-10">
    <div class="max-w-3xl opacity-0 animate-fade-in-up" style="animation-delay: 0.2s;">
      <span class="inline-block px-4 py-1 mb-6 text-sm font-bold tracking-wider text-primary uppercase bg-primary/10 backdrop-blur-md rounded-full border border-primary/20">
        Nutrición Consciente
      </span>
      
      <h1 class="text-6xl md:text-7xl lg:text-8xl font-bold text-white mb-4 leading-tight tracking-tight">
        Come bien,<br />
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary to-leaf">vive mejor.</span>
      </h1>

      <p class="text-2xl md:text-3xl text-primary font-bold italic mb-8 opacity-0 animate-fade-in-up" style="animation-delay: 0.3s;">
        Nutrición real, esfuerzo cero
      </p>

      <p class="text-xl md:text-2xl text-gray-200 mb-10 font-light leading-relaxed max-w-2xl opacity-0 animate-fade-in-up" style="animation-delay: 0.5s;">
        Platos diseñados por expertos, cocinados con pasión y entregados listos para disfrutar. Sin complicaciones.
      </p>
      
      <div class="flex flex-wrap gap-6 opacity-0 animate-fade-in-up" style="animation-delay: 0.6s;">
        <a
          href="/productos"
          class="group relative px-8 py-4 bg-primary text-white font-bold rounded-full overflow-hidden transition-all hover:shadow-[0_0_40px_-10px_rgba(var(--color-primary),0.5)]"
        >
          <span class="relative z-10">Ver Menú</span>
          <div class="absolute inset-0 h-full w-full scale-0 rounded-full transition-all duration-300 group-hover:scale-100 group-hover:bg-leaf"></div>
        </a>
        
        <a
          href="/planificador"
          class="group px-8 py-4 bg-white/10 backdrop-blur-md text-white font-bold rounded-full border border-white/20 hover:bg-white hover:text-gray-900 transition-all duration-300"
        >
          Planificar Semana
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes slow-zoom {
    from { transform: scale(1); }
    to { transform: scale(1.1); }
  }

  .animate-fade-in-up {
    animation: fade-in-up 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .animate-slow-zoom {
    animation: slow-zoom 20s linear infinite alternate;
  }
</style>
