---
// Página principal de NutreTerra
import Layout from '@/layouts/Layout.astro';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
import Hero from '@/components/Hero.astro';
import ProductCard from '@/components/ProductCard.astro';
import CategoryCard from '@/components/CategoryCard.astro';

import { getProducts, getCategories, getProductLines } from '@/lib/api';

// Obtener productos destacados desde la API
const featuredProducts = await getProducts({ featured: true, limit: 8 });

// Obtener categorías desde la API
const categories = await getCategories();

// Obtener líneas de producto activas
const productLines = (await getProductLines()).filter(line => line.active);
---

<Layout title="NutreTerra - Productos Naturales y Saludables">
  <Header />

  <!-- Hero Section -->
  <Hero />

  <!-- Productos Destacados -->
  <section class="py-16 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Productos <span class="text-primary">Destacados</span>
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Descubre nuestra selección de platos preparados más populares
        </p>
      </div>

      <!-- Grid de Productos - Compacto -->
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6">
        {featuredProducts.map((product) => (
          <a href={`/producto/${product.slug}`} class="group">
            <div class="bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-100 h-full flex flex-col">
              <div class="relative overflow-hidden aspect-square">
                <img
                  src={product.imageUrl}
                  alt={product.name}
                  class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                />
                {product.compareAtPrice && (
                  <div class="absolute top-2 right-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded-full">
                    OFERTA
                  </div>
                )}
              </div>
              <div class="p-4 flex-1 flex flex-col">
                <h3 class="font-semibold text-gray-900 mb-2 text-sm md:text-base line-clamp-2 group-hover:text-primary transition-colors">
                  {product.name}
                </h3>
                {product.calories && (
                  <p class="text-xs text-gray-500 mb-2">
                    {product.calories} kcal • {product.protein}g proteína
                  </p>
                )}
                <div class="mt-auto">
                  <div class="flex items-baseline gap-2">
                    <span class="text-lg md:text-xl font-bold text-primary">
                      {product.price.toFixed(2)}€
                    </span>
                    {product.compareAtPrice && (
                      <span class="text-sm text-gray-400 line-through">
                        {product.compareAtPrice.toFixed(2)}€
                      </span>
                    )}
                  </div>
                </div>
              </div>
            </div>
          </a>
        ))}
      </div>

      <!-- Ver todos -->
      <div class="text-center mt-12">
        <a href="/productos" class="inline-block px-8 py-4 bg-primary text-white font-bold rounded-lg hover:bg-leaf transition-all transform hover:scale-105 shadow-lg">
          Ver Todos Nuestros Productos
        </a>
      </div>
    </div>
  </section>

  <!-- Cómo Funciona - Dos Formas de Comprar -->
  <section class="py-16 bg-gradient-to-br from-primary/5 via-leaf/5 to-cream/30">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-gray-900 mb-4">
          ¿Cansado de <span class="text-primary">Planificar tus Comidas</span>?
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-4">
          Nosotros nos encargamos. Elige la forma que mejor se adapte a ti:
        </p>
        <p class="text-lg text-gray-500 max-w-2xl mx-auto">
          Puedes mezclar ambas opciones como prefieras
        </p>
      </div>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 max-w-5xl mx-auto mb-12">
        <!-- Opción 1: Planificador -->
        <div class="relative group">
          <div class="bg-white rounded-3xl shadow-2xl p-10 hover:shadow-3xl transition-all duration-500 border-2 border-transparent hover:border-primary/30 h-full flex flex-col">
            <div class="absolute -top-5 -right-5 w-16 h-16 bg-gradient-to-br from-primary to-leaf rounded-2xl flex items-center justify-center text-white font-bold text-2xl shadow-xl group-hover:scale-110 transition-transform duration-300">
              1
            </div>

            <div class="w-24 h-24 bg-gradient-to-br from-primary to-leaf rounded-3xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:rotate-3 transition-all duration-500 shadow-xl">
              <svg class="w-14 h-14 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
              </svg>
            </div>

            <h3 class="text-3xl font-bold text-gray-900 mb-4 text-center group-hover:text-primary transition-colors">
              Planificador Semanal
            </h3>

            <p class="text-gray-600 text-center leading-relaxed text-lg mb-6 flex-1">
              <span class="font-semibold text-primary">Arrastra y suelta</span> los platos en nuestro calendario semanal.
              Visualiza tus <span class="font-semibold">macros totales</span> y asegúrate de cumplir tus objetivos nutricionales.
              Perfecto si te gusta <span class="font-semibold">planificar con anticipación</span>.
            </p>

            <ul class="space-y-3 mb-8">
              <li class="flex items-center gap-3 text-gray-700">
                <svg class="w-6 h-6 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Vista semanal completa</span>
              </li>
              <li class="flex items-center gap-3 text-gray-700">
                <svg class="w-6 h-6 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Contador de macros automático</span>
              </li>
              <li class="flex items-center gap-3 text-gray-700">
                <svg class="w-6 h-6 text-primary flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Guarda y reutiliza tus menús</span>
              </li>
            </ul>

            <a href="/planificador" class="block w-full px-8 py-4 bg-gradient-to-r from-primary to-leaf text-white font-bold rounded-xl hover:shadow-2xl transition-all transform hover:scale-105 text-center text-lg">
              🗓️ Crear Mi Menú Semanal
            </a>
          </div>
        </div>

        <!-- Opción 2: Catálogo -->
        <div class="relative group">
          <div class="bg-white rounded-3xl shadow-2xl p-10 hover:shadow-3xl transition-all duration-500 border-2 border-transparent hover:border-leaf/30 h-full flex flex-col">
            <div class="absolute -top-5 -right-5 w-16 h-16 bg-gradient-to-br from-leaf to-accent rounded-2xl flex items-center justify-center text-white font-bold text-2xl shadow-xl group-hover:scale-110 transition-transform duration-300">
              2
            </div>

            <div class="w-24 h-24 bg-gradient-to-br from-leaf to-accent rounded-3xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:rotate-3 transition-all duration-500 shadow-xl">
              <svg class="w-14 h-14 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
              </svg>
            </div>

            <h3 class="text-3xl font-bold text-gray-900 mb-4 text-center group-hover:text-leaf transition-colors">
              Nuestros Platos
            </h3>

            <p class="text-gray-600 text-center leading-relaxed text-lg mb-6 flex-1">
              Navega por nuestro <span class="font-semibold text-leaf">catálogo completo</span> de platos preparados.
              Filtra por categoría o línea de producto y añade al carrito lo que más te apetezca.
              Ideal si prefieres <span class="font-semibold">comprar sobre la marcha</span>.
            </p>

            <ul class="space-y-3 mb-8">
              <li class="flex items-center gap-3 text-gray-700">
                <svg class="w-6 h-6 text-leaf flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Filtros por categoría y línea</span>
              </li>
              <li class="flex items-center gap-3 text-gray-700">
                <svg class="w-6 h-6 text-leaf flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Búsqueda rápida de platos</span>
              </li>
              <li class="flex items-center gap-3 text-gray-700">
                <svg class="w-6 h-6 text-leaf flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                <span>Compra solo lo que necesitas</span>
              </li>
            </ul>

            <a href="/productos" class="block w-full px-8 py-4 bg-gradient-to-r from-leaf to-accent text-white font-bold rounded-xl hover:shadow-2xl transition-all transform hover:scale-105 text-center text-lg">
              🍽️ Ver Todos Nuestros Platos
            </a>
          </div>
        </div>
      </div>

      <!-- Entrega -->
      <div class="text-center max-w-3xl mx-auto bg-white rounded-2xl shadow-xl p-8">
        <div class="w-16 h-16 bg-gradient-to-br from-accent to-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-4">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"></path>
          </svg>
        </div>
        <h3 class="text-2xl font-bold text-gray-900 mb-3">Recibe en 24-48h</h3>
        <p class="text-gray-600 text-lg">
          Sea cual sea la opción que elijas, tus comidas frescas y saludables llegarán a tu puerta.
          <span class="font-semibold text-primary">Envío gratis desde 50€</span>.
        </p>
      </div>
    </div>
  </section>

  <!-- Líneas de Producto -->
  <section class="py-20 bg-gradient-to-br from-cream/50 to-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-gray-900 mb-4">
          Nuestras <span class="bg-gradient-to-r from-primary to-leaf bg-clip-text text-transparent">Líneas de Producto</span>
        </h2>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto">
          Explora nuestras colecciones especializadas diseñadas para tu estilo de vida
        </p>
      </div>

      <!-- Grid de Líneas -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-6xl mx-auto">
        {productLines.map((line, index) => {
          const gradients = [
            'from-orange-100 to-amber-50 hover:from-orange-200 hover:to-amber-100',
            'from-green-100 to-emerald-50 hover:from-green-200 hover:to-emerald-100',
            'from-blue-100 to-cyan-50 hover:from-blue-200 hover:to-cyan-100',
            'from-purple-100 to-fuchsia-50 hover:from-purple-200 hover:to-fuchsia-100',
          ];
          const borderColors = [
            'border-orange-200 group-hover:border-orange-400',
            'border-green-200 group-hover:border-green-400',
            'border-blue-200 group-hover:border-blue-400',
            'border-purple-200 group-hover:border-purple-400',
          ];
          const textColors = [
            'text-orange-800 group-hover:text-orange-900',
            'text-green-800 group-hover:text-green-900',
            'text-blue-800 group-hover:text-blue-900',
            'text-purple-800 group-hover:text-purple-900',
          ];
          const iconColors = [
            'text-orange-600',
            'text-green-600',
            'text-blue-600',
            'text-purple-600',
          ];

          return (
            <a
              href={`/linea/${line.slug}`}
              class={`group relative bg-gradient-to-br ${gradients[index % 4]} rounded-3xl shadow-lg hover:shadow-2xl transition-all duration-500 overflow-hidden border-2 ${borderColors[index % 4]} transform hover:-translate-y-2 flex flex-col h-full`}
            >
              <div class="absolute top-0 right-0 -mt-10 -mr-10 w-40 h-40 bg-white/40 rounded-full blur-3xl group-hover:scale-150 transition-transform duration-700"></div>
              
              <div class="p-8 relative z-10 flex flex-col flex-grow">
                <div class="flex items-start justify-between mb-6">
                  <div
                    class={`w-16 h-16 ${iconColors[index % 4]} group-hover:scale-110 group-hover:rotate-6 transition-all duration-500 bg-white/60 rounded-2xl p-3 shadow-sm backdrop-blur-sm`}
                    set:html={line.icon}
                  />
                  {line.productCount && line.productCount > 0 && (
                    <span class="bg-white/80 backdrop-blur-sm px-3 py-1 rounded-full text-xs font-bold text-gray-600 shadow-sm border border-white/50">
                      {line.productCount} productos
                    </span>
                  )}
                </div>

                <h3 class={`text-3xl font-black mb-3 ${textColors[index % 4]} transition-colors duration-300 tracking-tight`}>
                  {line.name}
                </h3>

                <p class="text-gray-700 mb-8 leading-relaxed text-lg font-medium opacity-90 flex-grow">
                  {line.description}
                </p>

                <div class="flex items-center gap-2 font-bold text-gray-800 group-hover:gap-4 transition-all duration-300 mt-auto">
                  <span>Descubrir Línea</span>
                  <svg class="w-6 h-6 transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                  </svg>
                </div>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </section>

  <!-- Categorías -->
  <section class="section">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
          Explora por <span class="text-primary">Categoría</span>
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
          Encuentra exactamente lo que buscas navegando por nuestras categorías.
        </p>
      </div>

      <!-- Grid de Categorías -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-5xl mx-auto">
        {categories.map((category) => (
          <CategoryCard category={category} />
        ))}
      </div>
    </div>
  </section>

  <!-- Por qué elegirnos -->
  <section class="py-20 bg-gradient-to-br from-white via-primary/5 to-leaf/5">
    <div class="container mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-5xl font-bold text-gray-900 mb-6">
          ¿Por qué elegir <span class="bg-gradient-to-r from-primary to-leaf bg-clip-text text-transparent">NutreTerra</span>?
        </h2>
        <p class="text-xl text-gray-600 max-w-3xl mx-auto">
          Más que comida saludable, es tu aliado para una vida equilibrada
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <!-- Feature 1 -->
        <div class="group relative bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-500 p-8 text-center transform hover:-translate-y-2 border-2 border-transparent hover:border-primary/20">
          <div class="absolute inset-0 bg-gradient-to-br from-primary/5 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          <div class="relative z-10">
            <div class="w-20 h-20 bg-gradient-to-br from-primary to-primary/60 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:rotate-6 transition-all duration-500 shadow-lg">
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-primary transition-colors">100% Saludable</h3>
            <p class="text-gray-600 leading-relaxed">Ingredientes naturales, sin conservantes ni procesados. Certificación nutricional en cada plato.</p>
          </div>
        </div>

        <!-- Feature 2 -->
        <div class="group relative bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-500 p-8 text-center transform hover:-translate-y-2 border-2 border-transparent hover:border-leaf/20">
          <div class="absolute inset-0 bg-gradient-to-br from-leaf/5 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          <div class="relative z-10">
            <div class="w-20 h-20 bg-gradient-to-br from-leaf to-leaf/60 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:rotate-6 transition-all duration-500 shadow-lg">
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-leaf transition-colors">Ahorra Tiempo</h3>
            <p class="text-gray-600 leading-relaxed">Olvídate de cocinar y planificar. Platos listos en minutos para que disfrutes tu tiempo libre.</p>
          </div>
        </div>

        <!-- Feature 3 -->
        <div class="group relative bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-500 p-8 text-center transform hover:-translate-y-2 border-2 border-transparent hover:border-accent/20">
          <div class="absolute inset-0 bg-gradient-to-br from-accent/5 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          <div class="relative z-10">
            <div class="w-20 h-20 bg-gradient-to-br from-accent to-accent/60 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:rotate-6 transition-all duration-500 shadow-lg">
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-accent transition-colors">Control de Macros</h3>
            <p class="text-gray-600 leading-relaxed">Información nutricional detallada. Alcanza tus objetivos con precisión y sin complicaciones.</p>
          </div>
        </div>

        <!-- Feature 4 -->
        <div class="group relative bg-white rounded-2xl shadow-lg hover:shadow-2xl transition-all duration-500 p-8 text-center transform hover:-translate-y-2 border-2 border-transparent hover:border-primary/20">
          <div class="absolute inset-0 bg-gradient-to-br from-primary/5 to-transparent rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
          <div class="relative z-10">
            <div class="w-20 h-20 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl flex items-center justify-center mx-auto mb-6 group-hover:scale-110 group-hover:rotate-6 transition-all duration-500 shadow-lg">
              <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"></path>
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-3 group-hover:text-blue-600 transition-colors">Envío Express</h3>
            <p class="text-gray-600 leading-relaxed">Entrega en 24-48h. Envío gratis desde 50€. Tus comidas frescas cuando las necesites.</p>
          </div>
        </div>
      </div>

      <!-- CTA adicional -->
      <div class="mt-16 text-center">
        <div class="inline-block bg-white rounded-2xl shadow-xl p-8 max-w-2xl">
          <p class="text-2xl font-bold text-gray-900 mb-4">
            ¿Listo para empezar tu transformación saludable?
          </p>
          <p class="text-gray-600 mb-6">
            Únete a miles de personas que ya han mejorado su alimentación con NutreTerra
          </p>
          <a href="/planificador" class="inline-block px-10 py-4 bg-gradient-to-r from-primary to-leaf text-white font-bold rounded-xl hover:shadow-2xl transition-all transform hover:scale-105">
            Crear Mi Primer Menú Gratis
          </a>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>
