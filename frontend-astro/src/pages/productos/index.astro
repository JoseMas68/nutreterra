---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ProductCatalog from '../../islands/ProductCatalog';

const API_URL = import.meta.env.PUBLIC_API_URL || 'http://localhost:3001';
---

<Layout title="Todos los Productos - NutreTerra" description="Explora nuestro catálogo completo de platos preparados saludables">
  <Header />

  <main class="min-h-screen bg-gray-50">
    <!-- Header Section -->
    <section class="bg-white pt-16 pb-12 border-b border-gray-100">
      <div class="container mx-auto px-4">
        <h1 class="text-4xl md:text-6xl font-black text-gray-900 mb-6 tracking-tight">
          Nuestros Platos
        </h1>
        <p class="text-xl text-gray-600 max-w-3xl leading-relaxed">
          Descubre todos nuestros platos preparados saludables. Filtra por categoría o línea de producto para encontrar exactamente lo que buscas.
        </p>
      </div>
    </section>

    <!-- Catálogo con Filtros -->
    <ProductCatalog apiUrl={API_URL} client:load />
  </main>

  <Footer />
</Layout>
